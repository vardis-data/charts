apiVersion: v1
kind: Service
metadata:
  name: {{ include "api-docs.fullname" . }}
  labels:
    {{- include "api-docs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "api-docs.selectorLabels" . | nindent 4 }}
