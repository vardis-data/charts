suite: cloudflare secret tests
templates:
  - templates/cloudflare-secret.yaml
tests:
  - it: should render Secret when certManager is enabled
    asserts:
      - isKind:
          of: Secret
      - equal:
          path: metadata.name
          value: cloudflare-api-token

  - it: should render api token from values
    set:
      certManager.apiToken: "my-api-token"
    asserts:
      - equal:
          path: stringData.api-token
          value: "my-api-token"

  - it: should not render when certManager is disabled
    set:
      certManager.enabled: false
    asserts:
      - hasDocuments:
          count: 0

  - it: should have standard labels
    asserts:
      - equal:
          path: metadata.labels["app.kubernetes.io/name"]
          value: cloudflare-ingress
      - exists:
          path: metadata.labels["helm.sh/chart"]
