{{- if .Values.pdb.primary.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "pg-duckdb.fullname" . }}-primary
  labels:
    {{- include "pg-duckdb.labels" . | nindent 4 }}
spec:
  minAvailable: {{ .Values.pdb.primary.minAvailable }}
  selector:
    matchLabels:
      {{- include "pg-duckdb.selectorLabels" . | nindent 6 }}
      pg-duckdb/role: primary
{{- end }}
