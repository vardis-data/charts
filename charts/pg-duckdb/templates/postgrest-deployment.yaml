{{- if .Values.postgrest.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "pg-duckdb.fullname" . }}-postgrest
  labels:
    {{- include "pg-duckdb.labels" . | nindent 4 }}
    app.kubernetes.io/component: postgrest
spec:
  replicas: {{ .Values.postgrest.replicas }}
  selector:
    matchLabels:
      {{- include "pg-duckdb.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: postgrest
  template:
    metadata:
      labels:
        {{- include "pg-duckdb.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: postgrest
      {{- if .Values.reloader.enabled }}
      annotations:
        reloader.stakater.com/auto: "true"
      {{- end }}
    spec:
      initContainers:
        - name: setup-role
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: ["bash", "-c"]
          args:
            - |
              set -e

              until pg_isready -h "$PGHOST" -U "$PGUSER"; do
                echo "Waiting for primary database..."
                sleep 3
              done

              ROLE="{{ .Values.postgrest.anonRole }}"
              SCHEMAS="{{ .Values.postgrest.schemas }}"

              existing=$(PGPASSWORD="$PGPASSWORD" psql -tAc "SELECT 1 FROM pg_catalog.pg_roles WHERE rolname = '$ROLE'" 2>/dev/null || echo "")

              if [ "$existing" = "1" ]; then
                echo "Role $ROLE already exists, updating password..."
                PGPASSWORD="$PGPASSWORD" psql -c "ALTER ROLE \"$ROLE\" WITH LOGIN ENCRYPTED PASSWORD '$ANON_PASSWORD'; GRANT CONNECT ON DATABASE $PGDATABASE TO \"$ROLE\";"
              else
                echo "Creating role: $ROLE"
                PGPASSWORD="$PGPASSWORD" psql -c "CREATE ROLE \"$ROLE\" WITH LOGIN ENCRYPTED PASSWORD '$ANON_PASSWORD' NOINHERIT; GRANT CONNECT ON DATABASE $PGDATABASE TO \"$ROLE\";"
              fi

              IFS=',' read -ra SCHEMA_ARRAY <<< "$SCHEMAS"
              for schema in "${SCHEMA_ARRAY[@]}"; do
                schema=$(echo "$schema" | xargs)
                echo "Granting $ROLE access to schema: $schema"
                PGPASSWORD="$PGPASSWORD" psql -c "CREATE SCHEMA IF NOT EXISTS \"$schema\"; GRANT USAGE ON SCHEMA \"$schema\" TO \"$ROLE\"; GRANT SELECT ON ALL TABLES IN SCHEMA \"$schema\" TO \"$ROLE\"; ALTER DEFAULT PRIVILEGES IN SCHEMA \"$schema\" GRANT SELECT ON TABLES TO \"$ROLE\";"
              done

              echo "PostgREST role setup complete."
          env:
            - name: PGHOST
              value: {{ include "pg-duckdb.fullname" . }}
            - name: PGPORT
              value: {{ .Values.postgres.port | quote }}
            - name: PGDATABASE
              value: {{ .Values.postgres.database | quote }}
            - name: PGUSER
              value: {{ .Values.postgres.user | quote }}
            - name: PGPASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "pg-duckdb.passwordSecretName" . }}
                  key: {{ include "pg-duckdb.passwordKey" . }}
            - name: ANON_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "pg-duckdb.fullname" . }}-postgrest
                  key: anon-role-password
      containers:
        - name: postgrest
          image: "{{ .Values.postgrest.image.repository }}:{{ .Values.postgrest.image.tag }}"
          imagePullPolicy: {{ .Values.postgrest.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 3000
              protocol: TCP
            - name: admin
              containerPort: 3001
              protocol: TCP
          env:
            - name: PGRST_DB_URI
              valueFrom:
                secretKeyRef:
                  name: {{ include "pg-duckdb.fullname" . }}-postgrest
                  key: db-uri
            - name: PGRST_DB_ANON_ROLE
              value: {{ .Values.postgrest.anonRole | quote }}
            - name: PGRST_DB_SCHEMAS
              value: {{ .Values.postgrest.schemas | quote }}
            - name: PGRST_DB_CHANNEL_ENABLED
              value: "false"
            - name: PGRST_SERVER_PORT
              value: "3000"
            - name: PGRST_ADMIN_SERVER_PORT
              value: "3001"
            {{- if .Values.postgrest.jwt.enabled }}
            - name: PGRST_JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "pg-duckdb.postgrest.jwtSecretName" . }}
                  key: {{ include "pg-duckdb.postgrest.jwtSecretKey" . }}
            {{- end }}
          livenessProbe:
            httpGet:
              path: /live
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /ready
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
          {{- with .Values.postgrest.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
{{- end }}
