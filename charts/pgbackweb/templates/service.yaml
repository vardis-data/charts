apiVersion: v1
kind: Service
metadata:
  name: {{ include "pgbackweb.fullname" . }}
  labels:
    {{- include "pgbackweb.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pgbackweb.selectorLabels" . | nindent 4 }}
